# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# generated artifacts
# 运行时生成的文件
out/

# 数据备份目录
data/backup/

# macOS 生成的文件
.DS_Store

# 运行时生成的文件
assets/runtime/*.tar.gz

# macOS 平台随附的 JRE 目录（避免提交体积巨大的运行时）
assets/runtime/jdk-macos-jre/

# Camellia 运行时生成的文件
assets/camellia/*.jar

# redis-rdb-cli 运行时生成的文件
redis-rdb-cli/log/

# Editor/IDE
# .idea/
# .vscode/

# 忽略.github目录
camellia/.github*

# Go 构建缓存目录
.gocache/

# 旧版根目录二进制（现统一输出到 bin/）
/df2redis

# 忽略测试生成 macos版本二进制文件
./df2redis

# 本地参考源码与构建产物
dragonfly/
bin/
