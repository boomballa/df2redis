source:
  type: dragonfly
  addr: 10.46.128.12:7380
  password: ""
  tls: false

target:
  # type: redis-standalone or redis-cluster
  type: redis-cluster 
  seed: 10.180.50.231:6379
  password: "pwd4dba"
  tls: false

stateDir: ../out
statusFile: ../out/status.json

migrate:
  snapshotPath: ../data/backup/dragonfly7380-dump.rdb
  shakeBinary: ../redis-shake-v4/redis-shake
  shakeArgs: ""          # 选填：完整命令行参数（如不用配置文件）
  shakeConfigFile: ""    # 选填：redis-shake 配置文件路径（如使用 -conf）
  autoBgsave: false      # 是否在源端自动触发 BGSAVE
  bgsaveTimeoutSeconds: 300
